const SERVER_URL = "localhost:5000";

const CONSTANTS = {
  HTTP_SERVER_URL: `http://${SERVER_URL}`,
  REFRESH_TOKEN: "refreshToken",
  THEMES: {
    LIGHT_THEM: 'lightTheme',
    DARK_THEM: 'darkTheme'
  }
};

export default CONSTANTS;
